<table class='tab_cadre_central'>
   {{ hook_display_central() }}
   <tr>
      <th colspan="2">
         {% include 'central/messages.html.twig' with warnings|default({}) %}
      </th>
   </tr>
   <tr class="noHover">
      <td class="top" width="50%">
         <table class="central">
            <tr class="noHover">
               <td>
                  {% if have_rights_or('ticketvalidation', validation.getValidateRights()) %}
                     {# TODO Ticket::showCentralList(0, "tovalidate", false) #}
                  {% endif %}
                  {% if have_rights_or('ticket', {const('Ticket', 'READMY'), const('Ticket', 'READALL'), const('Ticket', 'READASSIGN')})) %}
                     {# TODO Ticket::showCentralList(0, "tovalidate", false) #}
                     {# TODO Ticket::showCentralList(0, "tovalidate", false) #}
                     {# TODO Ticket::showCentralList(0, "tovalidate", false) #}
                     {# TODO Ticket::showCentralList(0, "tovalidate", false) #}
                     {# TODO Ticket::showCentralList(0, "tovalidate", false) #}
                  {% endif %}
               </td>
            </tr>
         </table>
      </td>
   </tr>
</table>
