# Copy this file as .env.local and set your own values if needed.

# GLPI base URL
# Should be an url leading to a GLPI server running in the "e2e_testing" environment
GLPI_BASE_URL="http://host.docker.internal:8090"
